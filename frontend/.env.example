# Backend base URL. Use your LAN IP instead of localhost if you open the dashboard from another device.
VITE_API_URL="http://localhost:4000"

# The dashboard signs in with the seeded user. It does not embed WEBHOOK_API_KEY.
# An old VITE_API_KEY in this file is ignored.
